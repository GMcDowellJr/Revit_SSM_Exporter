# Golden artifacts

This folder contains the baseline outputs used for regression during refactor.

Rules:
- Do not replace baseline artifacts unless explicitly re-baselining.
- Any refactor step must match these outputs byte-for-byte (CSV), unless a change is declared.
