# SSM Exporter (v4)

## Ground rules
- PATCH-only by default.
- One intent per commit (MOVE ONLY vs BEHAVIOR CHANGE).
- Baseline commit is sacred.

## Golden runs
(Attach baseline CSV outputs and view set definitions here.)
